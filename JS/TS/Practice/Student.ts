export {Student};
class Student {
    public FullName: string;
    public Grade: number;

    public constructor(FullName: string, Grade: number) {
        this.FullName = FullName;
        this.Grade = Grade;
    }
}

